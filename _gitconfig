[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = Jason Conway
[core]
	excludesFile = ~/.gitignore
[init]
	defaultBranch = main
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
